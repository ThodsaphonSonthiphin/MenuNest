import type {RailAction} from './ShortcutRailProvider'

/** Stub — replaced in Task 3 by the real SpeedDial. */
export function ShortcutRail({actions}: {actions: RailAction[]}) {
  void actions
  return null
}
